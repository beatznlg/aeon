# AEON α — Python dependencies extracted from aeon.py
#
# Install with:
#   pip install -r requirements.txt
#
# Note: AEON α is intended to run in Google Colab on a T4 GPU. llama-cpp-python
# needs either prebuilt wheels matching your Python version, or a C++ toolchain.
# openai-whisper pulls in PyTorch. Expect a multi-minute install on a fresh
# Colab runtime.

# --- core LLM + retrieval ---
llama-cpp-python==0.2.90
sentence-transformers>=3.0
faiss-cpu>=1.8

# --- Telegram surface ---
pyTelegramBotAPI

# --- HTTP / async ---
httpx
requests

# --- data + config ---
pydantic>=2.5
zstandard
numpy

# --- system / sandbox ---
psutil

# --- math / symbolic ---
sympy

# --- speech / images ---
edge-tts
opencv-python

# --- optional / lazy ---
google-generativeai   # Gemini multimodal (vision + audio transcription)
web3                  # Base L2 wallet (only needed if WEB3_PRIVATE_KEY is set)
openai-whisper        # explicitly imported in the script (transcription fallback)
