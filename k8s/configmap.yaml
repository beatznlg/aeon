apiVersion: v1
kind: ConfigMap
metadata:
  name: aeon-config
  namespace: aeon
data:
  AEON_PYTHON_HOST: "0.0.0.0"
  AEON_PYTHON_PORT: "5000"
  AEON_LOG_LEVEL: "INFO"
  # Add other non-secret config here (e.g., SUPABASE_URL if public).
